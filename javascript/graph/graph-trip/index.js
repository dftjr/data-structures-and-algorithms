'use strict';

const graphTrip = (graph, arr) => {
    let index = 1;
    let price = 0;
    let connected = true;

    while (connected && index < arr.length) {
    // while the statement in "while" true,
    // itterate through the graphs to find the cost
    // return null if theres no connection
    // return the price
    };
};

module.exports = graphTrip;
